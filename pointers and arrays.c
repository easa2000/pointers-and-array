pointers and array

#include<stdio.h>
main()
{
    int i=1;
    int arr[] = {1,2,3,4,5};
    printf("\n Address of array = %p",(i+arr));
    // int *ptr = &arr[i];
    // printf("\n Value of first element : %d",*ptr);
}
